"""
Views do app core.

Endpoints serão adicionados nos próximos blocos do roadmap.

Healthcheck e status público vivem em `arminda/urls.py` (rotas
`/health/`, `/status/` e o alias `/api/health/`) — ali são funções
simples sem dependência de DRF, mais resilientes a falhas de import.
"""
